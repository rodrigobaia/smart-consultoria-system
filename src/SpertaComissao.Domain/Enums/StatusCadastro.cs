namespace SpertaComissao.Domain.Enums;

/// <summary>
/// Status genérico para cadastros (Loja, Produto, Usuário, etc.).
/// </summary>
public enum StatusCadastro
{
    Inativo = 0,
    Ativo = 1,
    Bloqueado = 2
}
